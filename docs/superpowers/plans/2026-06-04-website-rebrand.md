# Semagram Website Rebrand Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebrand the single-page marketing site around "Building general intelligence for the power industry" — vision-first layout, a capabilities-lead 3-pillar story, a light technical aesthetic, a cursor-reactive network-graph hero whose page-load construction *is* the intro animation, and a Space Grotesk / Inter / JetBrains Mono type system.

**Architecture:** Stays a single static `index.html` styled with compiled Tailwind (`assets/css/main.css`) and hand-written vanilla JS (`assets/js/main.js`), deployed via Wrangler. No framework, router, or backend added. New design tokens go in `tailwind.config.js`; new reusable styles in `src/css/tailwind.css` under `@layer`; new behavior in `assets/js/main.js`. After every CSS/config/class change you MUST run `npm run build-css` because the browser loads the committed `assets/css/main.css`, not the source. Tailwind's `content` scan is only `./index.html`, so every utility class must appear in `index.html` or it gets purged.

**Tech Stack:** HTML, Tailwind CSS (CLI build), vanilla JS (no modules), inline SVG, Google Fonts, ScrollReveal/Swiper/GLightbox (CDN, unchanged), Wrangler/Cloudflare.

**Verification model:** This project has **no test runner** (`npm test` is a stub that exits 1). Per repo conventions, each task is verified by (a) `npm run build-css` completing without error and (b) a visual smoke-test in the browser. "Run/Expected" steps below reflect that. Open the site during development with any static server, e.g. `npx serve .` then visit the printed URL, or open `index.html` directly. Use `npm run tw-build` in a separate terminal to auto-recompile while iterating.

**Branch:** Work on the current `cloudflare-deploy` branch (the repo's main branch) unless you prefer a feature branch. Commit after every task.

---

## File Structure

| File | Responsibility | Action |
|------|----------------|--------|
| `tailwind.config.js` | Design tokens: `electric` cyan color, `display`/`mono` font families | Modify |
| `src/css/tailwind.css` | Font imports + new `@layer` component/utility classes (grid-bg, eyebrow, gradient-text, pillar card, stack diagram, hero graph/spotlight/status, reveal, reduced-motion) | Modify |
| `assets/css/main.css` | Compiled output — regenerated by `npm run build-css`, committed | Generated |
| `index.html` | `<head>` meta/SEO + nav + all `<section>` markup | Modify (large) |
| `assets/js/main.js` | Graph self-construction intro + cursor-reactive hero + spotlight (replaces `.page-loading` logic); existing wiring kept | Modify |

Section order in `index.html` after the rebuild: `home` (hero) → `platform` → `pillars` → `solutions` (who it's for) → `clients` → `community` → `support` → `team` → `contact` → footer.

---

## Task 1: Design tokens + fonts

**Files:**
- Modify: `tailwind.config.js:9-21` (theme.extend colors + add fontFamily entries)
- Modify: `src/css/tailwind.css:2` (Google Fonts import)

- [ ] **Step 1: Add the cyan token and font families to the Tailwind theme**

In `tailwind.config.js`, replace the `fontFamily` and `colors` blocks inside `theme.extend` (currently lines 10-21) with:

```js
      fontFamily: {
        sans: ["Inter", "sans-serif"],
        display: ["Space Grotesk", "Inter", "sans-serif"],
        mono: ["JetBrains Mono", "ui-monospace", "monospace"],
      },
      colors: {
        "background-dark": "#f1f5f9",
        "background-light": "#ffffff",
        "background-medium": "#f8fafc",
        primary: "#0f172a",
        secondary: "#475569",
        accent: "#2563eb",
        electric: "#22d3ee",
        "border-color": "rgba(0, 0, 0, 0.1)",
      },
```

- [ ] **Step 2: Load Space Grotesk + JetBrains Mono alongside Inter**

In `src/css/tailwind.css`, replace the existing `@import` on line 2:

```css
@import url("https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap");
```

with:

```css
@import url("https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Space+Grotesk:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap");
```

- [ ] **Step 3: Build the CSS**

Run: `npm run build-css`
Expected: completes with no error; `assets/css/main.css` is regenerated (its mtime updates). No "unknown utility" warnings.

- [ ] **Step 4: Commit**

```bash
git add tailwind.config.js src/css/tailwind.css assets/css/main.css
git commit -m "feat: add electric color token and Space Grotesk/JetBrains Mono fonts"
```

---

## Task 2: New CSS component & utility classes

**Files:**
- Modify: `src/css/tailwind.css` (append new rules inside existing `@layer components` and `@layer utilities`)

- [ ] **Step 1: Add component classes**

In `src/css/tailwind.css`, inside the `@layer components { ... }` block, just before its closing `}` (currently line 373), add:

```css
  /* ---- Rebrand: grid background, labels, hero graph, pillars, stack ---- */
  .grid-bg {
    background-image:
      linear-gradient(rgba(15, 23, 42, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(15, 23, 42, 0.05) 1px, transparent 1px);
    background-size: 28px 28px;
  }

  .eyebrow {
    @apply font-mono text-xs font-medium tracking-wider text-accent uppercase;
  }

  .text-gradient {
    @apply bg-clip-text text-transparent;
    background-image: linear-gradient(90deg, #2563eb, #22d3ee);
  }

  /* Hero entrance: copy starts masked, slides up when .is-revealed is set */
  .reveal-mask {
    @apply block overflow-hidden;
  }
  .reveal-mask > span {
    @apply block;
    transform: translateY(110%);
  }
  .is-revealed .reveal-mask > span {
    transform: translateY(0);
    transition: transform 0.9s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .reveal-fade {
    @apply opacity-0 translate-y-3;
  }
  .is-revealed .reveal-fade {
    @apply opacity-100 translate-y-0;
    transition: all 0.7s ease 0.15s;
  }

  /* Hero network graph layers */
  .hero-graph {
    @apply absolute inset-0 h-full w-full;
  }
  .hero-spot {
    @apply pointer-events-none absolute rounded-full opacity-0;
    width: 520px;
    height: 520px;
    transform: translate(-50%, -50%);
    background: radial-gradient(circle, rgba(37, 99, 235, 0.12) 0%, rgba(34, 211, 238, 0.05) 35%, transparent 70%);
    mix-blend-mode: multiply;
    transition: opacity 0.3s ease;
  }
  .hero-status {
    @apply font-mono text-xs text-accent opacity-0;
    transition: opacity 0.4s ease;
  }
  .hero-status.is-on {
    @apply opacity-100;
  }

  /* Pillar cards */
  .pillar-card {
    @apply group relative overflow-hidden rounded-2xl border border-border-color bg-background-light p-8 shadow-card-1 transition-all duration-300 hover:-translate-y-1 hover:shadow-card-2;
  }

  /* Stack diagram rows (platform section) */
  .stack-row {
    @apply flex items-center rounded-xl px-5 py-4 font-medium;
  }
```

- [ ] **Step 2: Add reduced-motion guard inside `@layer utilities`**

In `src/css/tailwind.css`, inside the `@layer utilities { ... }` block, just before its closing `}` (currently line 439), add:

```css
  @media (prefers-reduced-motion: reduce) {
    .is-revealed .reveal-mask > span,
    .is-revealed .reveal-fade {
      transition: none;
    }
  }
```

- [ ] **Step 3: Build the CSS**

Run: `npm run build-css`
Expected: completes with no error. (These classes won't appear in output yet because none are used in `index.html`; that's fine — Tailwind keeps custom `@layer` rules whose selectors aren't utility-scanned. Verify no build error.)

- [ ] **Step 4: Commit**

```bash
git add src/css/tailwind.css assets/css/main.css
git commit -m "feat: add grid-bg, eyebrow, gradient, hero-graph, pillar, stack styles"
```

---

## Task 3: Head metadata + navigation

**Files:**
- Modify: `index.html:14-31` (meta/title/SEO copy)
- Modify: `index.html:84-113` (nav menu links)

- [ ] **Step 1: Update meta + title to the new story**

In `index.html`, replace the description/title/OG/Twitter block (lines 14-31) so all human-readable copy reflects the new positioning. Replace:

```html
  <meta name="description" content="Semagram Energy - Accelerating Access to Power Grids" />
  <meta name="keywords" content="energy, power grids, automation, interconnection, semagram" />
  <meta name="twitter:title" content="Semagram Energy | Accelerating Access to Power Grids" />
  <meta name="twitter:description" content="Semagram Energy - Accelerating Access to Power Grids" />
  <meta name="twitter:image" content="./assets/img/semagram-cover.png" />
  <meta content="Semagram Energy | Accelerating Access to Power Grids" property="og:title" />
  <meta content="Semagram Energy" property="og:site_name" />
  <meta content="Semagram Energy - Accelerating Access to Power Grids" property="og:description" />
  <meta content="./assets/img/semagram-cover.png" property="og:image" />
  <meta content="https://semagram.energy" property="og:url" />
  <meta content="website" property="og:type" />

  <meta name="msapplication-TileColor" content="#ffffff" />
  <meta name="msapplication-TileImage" content="./assets/favicon/mstile-144x144.png" />
  <meta name="theme-color" content="#ffffff" />

  <!-- Page title -->
  <title>Semagram Energy | Accelerating Access to Power Grids</title>
```

with:

```html
  <meta name="description" content="Semagram is building general intelligence for the power industry — the canonical data and reasoning layer for grid interconnection and planning." />
  <meta name="keywords" content="power grid AI, grid intelligence, interconnection, grid planning, canonical data model, power industry, semagram, poweragent" />
  <meta name="twitter:title" content="Semagram | General Intelligence for the Power Industry" />
  <meta name="twitter:description" content="The canonical data and reasoning layer for grid interconnection and planning. From the creators of PowerAgent." />
  <meta name="twitter:image" content="./assets/img/semagram-cover.png" />
  <meta content="Semagram | General Intelligence for the Power Industry" property="og:title" />
  <meta content="Semagram Energy" property="og:site_name" />
  <meta content="The canonical data and reasoning layer for grid interconnection and planning. From the creators of PowerAgent." property="og:description" />
  <meta content="./assets/img/semagram-cover.png" property="og:image" />
  <meta content="https://semagram.energy" property="og:url" />
  <meta content="website" property="og:type" />

  <meta name="msapplication-TileColor" content="#ffffff" />
  <meta name="msapplication-TileImage" content="./assets/favicon/mstile-144x144.png" />
  <meta name="theme-color" content="#ffffff" />

  <!-- Page title -->
  <title>Semagram | General Intelligence for the Power Industry</title>
```

- [ ] **Step 2: Update the nav menu links**

In `index.html`, replace the five `<li>` nav items (lines 88-112) with these (adds `Platform` + `Pillars`, keeps Community/Team/Contact, drops the now-merged `Support` top-level link to avoid a 6+ item desktop nav):

```html
          <li class="group relative">
            <a href="#platform"
              class="ic-page-scroll mx-8 flex py-2 text-base font-medium text-primary/80 group-hover:text-primary lg:mr-0 lg:inline-flex lg:px-0 lg:py-6"
              role="menuitem">Platform</a>
          </li>
          <li class="group relative">
            <a href="#pillars"
              class="ic-page-scroll mx-8 flex py-2 text-base font-medium text-primary/80 group-hover:text-primary lg:mr-0 lg:inline-flex lg:px-0 lg:py-6"
              role="menuitem">How it works</a>
          </li>
          <li class="group relative">
            <a href="#solutions"
              class="ic-page-scroll mx-8 flex py-2 text-base font-medium text-primary/80 group-hover:text-primary lg:mr-0 lg:inline-flex lg:px-0 lg:py-6"
              role="menuitem">Who it's for</a>
          </li>
          <li class="group relative">
            <a href="#community"
              class="ic-page-scroll mx-8 flex py-2 text-base font-medium text-primary/80 group-hover:text-primary lg:mr-0 lg:inline-flex lg:px-0 lg:py-6"
              role="menuitem">Community</a>
          </li>
          <li class="group relative">
            <a href="#team"
              class="ic-page-scroll mx-8 flex py-2 text-base font-medium text-primary/80 group-hover:text-primary lg:mr-0 lg:inline-flex lg:px-0 lg:py-6"
              role="menuitem">Team</a>
          </li>
          <li class="group relative">
            <a href="#contact"
              class="ic-page-scroll mx-8 flex py-2 text-base font-medium text-primary/80 group-hover:text-primary lg:mr-0 lg:inline-flex lg:px-0 lg:py-6"
              role="menuitem">Contact</a>
          </li>
```

- [ ] **Step 3: Update the "Contact Us" button label to "Request access"**

In `index.html`, in the header action button (line 117-119), change the link text `Contact Us` to `Request access` (keep the `mailto:contact@semagram.energy` href and all classes).

- [ ] **Step 4: Build and visually verify**

Run: `npm run build-css`
Then open the site in a browser.
Expected: build succeeds; browser tab title reads "Semagram | General Intelligence for the Power Industry"; desktop nav shows Platform / How it works / Who it's for / Community / Team / Contact; the header button reads "Request access". (Anchor targets `#platform`/`#pillars` don't exist yet — they will after Tasks 4 & 6; clicking is a no-op for now, which is expected.)

- [ ] **Step 5: Commit**

```bash
git add index.html assets/css/main.css
git commit -m "feat: update head metadata and nav for new positioning"
```

---

## Task 4: Hero section (markup) + remove old loader overlay

**Files:**
- Modify: `index.html:54-69` (delete the `.page-loading` overlay)
- Modify: `index.html:136-159` (replace the hero `<section id="home">`)

- [ ] **Step 1: Remove the old page-loading overlay**

In `index.html`, delete the entire `<!-- Page loading -->` block (lines 54-69), i.e. the `<div class="page-loading ...">...</div>` containing the `.grid-loader`. The new graph-construction intro replaces it.

- [ ] **Step 2: Replace the hero section markup**

In `index.html`, replace the whole `<section id="home"> ... </section>` (lines 136-159) with:

```html
    <!-- Hero section -->
    <section id="home"
      class="relative overflow-hidden bg-background-light pt-[120px] pb-[90px] lg:pt-[150px] lg:pb-[120px]">
      <!-- grid + interactive network graph background -->
      <div class="grid-bg pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-1000" data-hero-grid aria-hidden="true"></div>
      <div class="hero-spot" data-hero-spot aria-hidden="true"></div>
      <svg class="hero-graph pointer-events-none" data-hero-net viewBox="0 0 1000 540" preserveAspectRatio="xMidYMid slice" aria-hidden="true"></svg>

      <div class="container relative">
        <div class="hero-reveal mx-auto max-w-[1100px]" data-hero-reveal>
          <p class="eyebrow reveal-mask mb-5"><span>// from the creators of PowerAgent</span></p>
          <h1 class="font-display text-4xl font-bold leading-[1.05] tracking-tight text-primary sm:text-5xl lg:text-6xl">
            <span class="reveal-mask"><span>Building <span class="text-gradient">general intelligence</span></span></span>
            <span class="reveal-mask"><span>for the power industry</span></span>
          </h1>
          <p class="reveal-mask mt-7 max-w-[560px] text-lg text-secondary">
            <span>The canonical data and reasoning layer that sits above legacy solvers — and improves with every project.</span>
          </p>
          <div class="reveal-fade mt-9 flex flex-wrap gap-4">
            <a href="mailto:contact@semagram.energy"
              class="inline-flex items-center justify-center rounded-lg bg-accent px-7 py-3 text-base font-semibold text-white shadow-lg shadow-accent/30 transition-all hover:-translate-y-0.5 hover:bg-opacity-90">
              Request access
            </a>
            <a href="#platform"
              class="ic-page-scroll inline-flex items-center justify-center rounded-lg border border-border-color bg-background-light px-7 py-3 text-base font-semibold text-primary transition-all hover:border-accent hover:text-accent">
              See the platform
            </a>
          </div>
        </div>
      </div>

      <!-- construction status readout -->
      <div class="hero-status absolute bottom-8 left-0 right-0" data-hero-status aria-hidden="true">
        <div class="container">
          <span data-hero-status-text>initializing</span>
          <span class="text-secondary" data-hero-status-pct>0%</span>
        </div>
      </div>
    </section>
```

- [ ] **Step 3: Build and visually verify (static, pre-JS)**

Run: `npm run build-css`
Then open the site.
Expected: build succeeds. The hero shows the headline "Building general intelligence for the power industry" with "general intelligence" in a blue→cyan gradient, the eyebrow, subhead, and two buttons (Request access / See the platform). Because the entrance JS isn't added until Task 5, the copy may sit hidden under the reveal masks — that's expected; verify the markup/structure exists (use devtools to confirm the `[data-hero-net]` SVG and `[data-hero-reveal]` block are present). The page no longer shows the old square-dot grid loader.

- [ ] **Step 4: Commit**

```bash
git add index.html assets/css/main.css
git commit -m "feat: rebuild hero markup with network-graph layers; remove old loader"
```

---

## Task 5: Hero JS — graph self-construction intro + cursor-reactive behavior

**Files:**
- Modify: `assets/js/main.js:3-14` (replace the `.page-loading` block)
- Modify: `assets/js/main.js` (append hero module at end of file)

- [ ] **Step 1: Replace the old page-loading logic with a reveal trigger**

In `assets/js/main.js`, replace lines 3-14 (the `// Page loading` block) with:

```js
// Page loading — the hero graph construction (below) drives the intro.
// If the hero module isn't present (e.g. SVG missing), reveal copy immediately on load.
window.addEventListener("load", () => {
  const reveal = document.querySelector("[data-hero-reveal]");
  if (reveal && !document.querySelector("[data-hero-net]")) {
    reveal.classList.add("is-revealed");
  }
});
```

- [ ] **Step 2: Append the hero network-graph module to the end of `assets/js/main.js`**

Add this at the very end of `assets/js/main.js`:

```js
// ---- Hero: network graph self-construction + cursor-reactive interaction ----
(function heroGraph() {
  const svg = document.querySelector("[data-hero-net]");
  const section = document.getElementById("home");
  if (!svg || !section) return;

  const NS = "http://www.w3.org/2000/svg";
  const VW = 1000, VH = 540;
  const grid = section.querySelector("[data-hero-grid]");
  const spot = section.querySelector("[data-hero-spot]");
  const reveal = section.querySelector("[data-hero-reveal]");
  const status = section.querySelector("[data-hero-status]");
  const statusText = section.querySelector("[data-hero-status-text]");
  const statusPct = section.querySelector("[data-hero-status-pct]");
  const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  // Node positions biased to the right so they clear the left-aligned copy.
  const base = [
    [660,120],[780,180],[870,110],[915,250],[835,340],[720,400],
    [630,290],[710,250],[890,370],[690,200],[600,170],[800,300],
  ];
  const edges = [
    [0,7],[0,10],[10,6],[6,7],[7,1],[1,9],[1,2],[2,3],
    [3,4],[4,11],[11,8],[8,1],[6,5],[5,11],[4,8],
  ];
  const phases = [
    "indexing grid topology",
    "linking interconnection projects",
    "aligning to canonical model",
    "memory layer online",
  ];

  const nodes = base.map(([x, y]) => ({ x, y, ox: x, oy: y, vx: 0, vy: 0 }));
  const dotEls = [];
  const lineEls = [];
  const mouse = { x: -9999, y: -9999 };
  let interactive = false;
  let raf = null;

  // Build SVG: lines first (under nodes), then nodes.
  edges.forEach((e) => {
    const a = base[e[0]], b = base[e[1]];
    const l = document.createElementNS(NS, "line");
    const len = Math.hypot(a[0] - b[0], a[1] - b[1]);
    l.setAttribute("x1", a[0]); l.setAttribute("y1", a[1]);
    l.setAttribute("x2", b[0]); l.setAttribute("y2", b[1]);
    l.setAttribute("stroke", "#2563eb");
    l.setAttribute("stroke-width", "1.4");
    l.setAttribute("stroke-dasharray", len);
    l.setAttribute("stroke-dashoffset", reduceMotion ? 0 : len);
    l.setAttribute("opacity", reduceMotion ? "0.28" : "0");
    l.style.transition = "stroke-dashoffset 0.5s ease, opacity 0.5s ease";
    svg.appendChild(l);
    lineEls.push({ el: l });
  });
  base.forEach((p, i) => {
    const big = i % 4 === 0;
    const c = document.createElementNS(NS, "circle");
    c.setAttribute("cx", p[0]); c.setAttribute("cy", p[1]);
    c.setAttribute("r", reduceMotion ? (big ? 7 : 4) : 0);
    c.setAttribute("fill", big ? "#1e3a8a" : "#2563eb");
    c.dataset.r = big ? 7 : 4;
    c.style.transition = "r 0.45s cubic-bezier(0.34, 1.56, 0.64, 1)";
    svg.appendChild(c);
    dotEls.push(c);
  });

  // Pointer tracking (used once interactive).
  section.addEventListener("pointermove", (e) => {
    const r = section.getBoundingClientRect();
    mouse.x = ((e.clientX - r.left) / r.width) * VW;
    mouse.y = ((e.clientY - r.top) / r.height) * VH;
    if (interactive && spot) {
      spot.style.opacity = "1";
      spot.style.left = (e.clientX - r.left) + "px";
      spot.style.top = (e.clientY - r.top) + "px";
    }
  });
  section.addEventListener("pointerleave", () => {
    mouse.x = mouse.y = -9999;
    if (spot) spot.style.opacity = "0";
  });

  const dist = (a, b, c, d) => Math.hypot(a - c, b - d);

  // Live cursor-reactive loop.
  let pulseEdge = 0, pulseT = 0, pulseDot = null;
  function loop() {
    nodes.forEach((n) => {
      let ax = (n.ox - n.x) * 0.02, ay = (n.oy - n.y) * 0.02;
      const d = dist(n.x, n.y, mouse.x, mouse.y);
      if (d < 200) {
        const f = ((200 - d) / 200) * 0.55;
        ax += ((mouse.x - n.x) / d) * f;
        ay += ((mouse.y - n.y) / d) * f;
      }
      n.vx = (n.vx + ax) * 0.86; n.vy = (n.vy + ay) * 0.86;
      n.x += n.vx; n.y += n.vy;
    });
    dotEls.forEach((c, i) => { c.setAttribute("cx", nodes[i].x); c.setAttribute("cy", nodes[i].y); });
    edges.forEach((e, i) => {
      const a = nodes[e[0]], b = nodes[e[1]];
      const mx = (a.x + b.x) / 2, my = (a.y + b.y) / 2;
      const d = dist(mx, my, mouse.x, mouse.y);
      const near = d < 170;
      const l = lineEls[i].el;
      l.setAttribute("x1", a.x); l.setAttribute("y1", a.y);
      l.setAttribute("x2", b.x); l.setAttribute("y2", b.y);
      l.setAttribute("stroke", near ? "#22d3ee" : "#2563eb");
      l.setAttribute("opacity", near ? (0.28 + ((170 - d) / 170) * 0.55) : 0.28);
      l.setAttribute("stroke-width", near ? "1.9" : "1.4");
    });
    if (!pulseDot) {
      pulseDot = document.createElementNS(NS, "circle");
      pulseDot.setAttribute("r", "3.5");
      pulseDot.setAttribute("fill", "#22d3ee");
      svg.appendChild(pulseDot);
    }
    pulseT += 0.012;
    if (pulseT >= 1) { pulseT = 0; pulseEdge = (pulseEdge + 1) % edges.length; }
    const ea = nodes[edges[pulseEdge][0]], eb = nodes[edges[pulseEdge][1]];
    pulseDot.setAttribute("cx", ea.x + (eb.x - ea.x) * pulseT);
    pulseDot.setAttribute("cy", ea.y + (eb.y - ea.y) * pulseT);
    raf = requestAnimationFrame(loop);
  }

  function goInteractive() {
    interactive = true;
    loop();
  }

  // Reduced motion: show finished graph + copy immediately, no construction.
  if (reduceMotion) {
    if (grid) grid.style.opacity = "1";
    if (reveal) reveal.classList.add("is-revealed");
    goInteractive();
    return;
  }

  // Construction timeline (drives the page-load intro).
  const timers = [];
  const T = (fn, ms) => timers.push(setTimeout(fn, ms));

  window.addEventListener("load", () => {
    T(() => { if (grid) grid.style.opacity = "1"; if (status) status.classList.add("is-on"); }, 150);

    // Pop nodes in.
    base.forEach((p, i) => T(() => { dotEls[i].setAttribute("r", dotEls[i].dataset.r); }, 250 + i * 70));

    // Draw edges after nodes.
    const eStart = 250 + base.length * 70 + 150;
    edges.forEach((e, i) => T(() => {
      const l = lineEls[i].el;
      l.setAttribute("opacity", "0.28");
      l.setAttribute("stroke-dashoffset", "0");
    }, eStart + i * 90));

    // Status readout counter.
    const total = eStart + edges.length * 90;
    let p = 0;
    const iv = setInterval(() => {
      p = Math.min(100, p + Math.ceil(Math.random() * 7) + 2);
      if (statusPct) statusPct.textContent = p + "%";
      if (statusText) statusText.textContent = phases[Math.min(phases.length - 1, Math.floor((p / 100) * phases.length))];
      if (p >= 100) clearInterval(iv);
    }, Math.max(60, total / 40));

    // Finish: hide status, reveal copy, hand off to interactive loop.
    T(() => { if (status) status.classList.remove("is-on"); }, total + 200);
    T(() => { if (reveal) reveal.classList.add("is-revealed"); }, total + 450);
    T(() => { goInteractive(); }, total + 700);
  });
})();
```

- [ ] **Step 3: Build (no CSS change, but keep artifacts in sync) and visually verify the intro**

Run: `npm run build-css`
Then hard-reload the site in a browser.
Expected: On load, the faint grid fades in, a mono readout appears bottom-left cycling `indexing grid topology → ... → memory layer online` with a `%` counter, nodes pop in, edges draw sequentially, then the headline lines slide up from their masks and the buttons fade in. After ~2.5s, moving the cursor over the hero pushes nodes around, lights nearby edges cyan, and shows the spotlight; a cyan pulse travels the network continuously.

- [ ] **Step 4: Verify reduced-motion fallback**

In the browser devtools, enable "Emulate prefers-reduced-motion: reduce" (Rendering tab) and reload.
Expected: the finished graph and hero copy appear immediately with no node-pop/edge-draw/pulse construction; cursor interaction still works.

- [ ] **Step 5: Commit**

```bash
git add assets/js/main.js assets/css/main.css
git commit -m "feat: graph self-construction intro + cursor-reactive hero graph"
```

---

## Task 6: Platform / "How it works" section (the Stack)

**Files:**
- Modify: `index.html` (insert new `<section id="platform">` immediately after the hero `</section>`, before `<!-- Solutions Section -->`)

- [ ] **Step 1: Insert the Platform section**

In `index.html`, directly after the hero section's closing `</section>` and before the `<!-- Solutions Section -->` comment, insert:

```html
    <!-- Platform / How it works -->
    <section id="platform" class="section-area relative overflow-hidden bg-background-medium">
      <div class="grid-bg pointer-events-none absolute inset-0 opacity-40" aria-hidden="true"></div>
      <div class="container relative z-10">
        <div class="scroll-revealed mx-auto mb-12 max-w-[860px] text-center">
          <p class="eyebrow mb-4">// the platform</p>
          <h2 class="font-display text-3xl font-bold text-primary md:text-4xl">
            One intelligence layer <span class="text-gradient">above your solvers</span>
          </h2>
          <p class="mx-auto mt-4 max-w-2xl text-lg text-secondary">
            Semagram doesn't replace PSS&#47;E, PSCAD, or your in-house tools. It sits above them — orchestrating the
            right workflows for each project, modeling from scratch where needed, and getting smarter with every study.
          </p>
        </div>

        <div class="scroll-revealed mx-auto flex max-w-2xl flex-col gap-3">
          <div class="stack-row bg-gradient-to-r from-accent to-blue-600 text-white shadow-lg">
            <span class="font-display text-base font-bold">Self-improving agent</span>
            <span class="ml-auto font-mono text-xs text-white/70">orchestration</span>
          </div>
          <div class="stack-row border border-border-color bg-blue-100/60 text-primary">
            <span class="font-semibold">Structured memory layer</span>
            <span class="ml-auto font-mono text-xs text-secondary">cross-project recall</span>
          </div>
          <div class="stack-row border border-border-color bg-blue-50 text-primary">
            <span class="font-semibold">Canonical data model</span>
            <span class="ml-auto font-mono text-xs text-secondary">interconnection &amp; planning</span>
          </div>
          <div class="stack-row border border-dashed border-secondary/40 text-secondary">
            <span class="font-mono text-sm">Legacy solvers &middot; PSS&#47;E &middot; PSCAD &middot; in-house tools</span>
          </div>
        </div>
      </div>
    </section>
```

- [ ] **Step 2: Build and visually verify**

Run: `npm run build-css`
Then open the site and scroll to the Platform section (also click "See the platform" in the hero).
Expected: build succeeds; a centered section shows the eyebrow "// the platform", the heading with "above your solvers" in gradient, intro copy, and a 4-row stack (agent → memory → canonical model → legacy solvers) with the top row in the blue gradient. The hero "See the platform" button and the "Platform" nav item now smooth-scroll here.

- [ ] **Step 3: Commit**

```bash
git add index.html assets/css/main.css
git commit -m "feat: add Platform section with stack diagram"
```

---

## Task 7: The 3 Pillars section

**Files:**
- Modify: `index.html` (insert new `<section id="pillars">` immediately after the Platform `</section>`)

- [ ] **Step 1: Insert the Pillars section**

In `index.html`, directly after the Platform section's closing `</section>`, insert:

```html
    <!-- The 3 Pillars -->
    <section id="pillars" class="section-area bg-background-light">
      <div class="container">
        <div class="scroll-revealed mx-auto mb-12 max-w-[860px] text-center">
          <p class="eyebrow mb-4">// how it works</p>
          <h2 class="font-display text-3xl font-bold text-primary md:text-4xl">
            Three layers that compound into <span class="text-gradient">general intelligence</span>
          </h2>
        </div>

        <div class="grid grid-cols-1 gap-6 md:grid-cols-3">
          <!-- Pillar 1 -->
          <div class="pillar-card scroll-revealed">
            <div class="interactive-glow"></div>
            <div class="relative z-10">
              <p class="eyebrow mb-4">01 &middot; data</p>
              <h3 class="font-display text-xl font-bold text-primary">Canonical data model</h3>
              <p class="mt-3 text-secondary">
                The shared model for grid interconnection and planning. Sits above legacy solvers, orchestrates the
                right workflow for each project and problem with proprietary solver technology, and models projects
                from scratch.
              </p>
            </div>
          </div>
          <!-- Pillar 2 -->
          <div class="pillar-card scroll-revealed">
            <div class="interactive-glow"></div>
            <div class="relative z-10">
              <p class="eyebrow mb-4">02 &middot; memory</p>
              <h3 class="font-display text-xl font-bold text-primary">Structured memory layer</h3>
              <p class="mt-3 text-secondary">
                A self-improving agent with structured memory across every project. Each study sharpens the next —
                the system compounds instead of starting over.
              </p>
            </div>
          </div>
          <!-- Pillar 3 -->
          <div class="pillar-card scroll-revealed">
            <div class="interactive-glow"></div>
            <div class="relative z-10">
              <p class="eyebrow mb-4">03 &middot; community</p>
              <h3 class="font-display text-xl font-bold text-primary">Community-focused</h3>
              <p class="mt-3 text-secondary">
                Human in the loop, by design. Built in the open by the creators of PowerAgent — the first open-source
                community for the power industry.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
```

- [ ] **Step 2: Wire the pillar cards into the existing cursor-glow handler**

The existing `initTabs()` in `assets/js/main.js` only attaches the `--x/--y` glow to `.tabs`. Add a small standalone handler so `.pillar-card` glows too. Append to the end of `assets/js/main.js`:

```js
// Pillar card cursor glow (reuses .interactive-glow --x/--y vars)
document.querySelectorAll(".pillar-card").forEach((card) => {
  card.addEventListener("mousemove", (e) => {
    const rect = card.getBoundingClientRect();
    const glow = card.querySelector(".interactive-glow");
    if (!glow) return;
    glow.style.setProperty("--x", (e.clientX - rect.left) + "px");
    glow.style.setProperty("--y", (e.clientY - rect.top) + "px");
  });
});
```

- [ ] **Step 3: Build and visually verify**

Run: `npm run build-css`
Then open the site and scroll to Pillars (also click "How it works" in the nav).
Expected: build succeeds; three cards titled "Canonical data model", "Structured memory layer", "Community-focused" with mono eyebrows (01 · data, etc.). Hovering a card lifts it and shows a soft blue radial glow that follows the cursor.

- [ ] **Step 4: Commit**

```bash
git add index.html assets/js/main.js assets/css/main.css
git commit -m "feat: add 3 Pillars section with cursor-glow cards"
```

---

## Task 8: Reframe the "Who it's for" (Solutions) section

**Files:**
- Modify: `index.html` (Solutions section header — originally lines 164-174, shifted down by Tasks 6-7)

- [ ] **Step 1: Update the Solutions section header copy + heading font**

In `index.html`, find the Solutions section header block (the `<div class="scroll-revealed mx-auto mb-12 max-w-[1000px] text-center">` containing `Solutions for <span class="text-accent">Every Stakeholder</span>`). Replace that inner block with:

```html
        <div class="scroll-revealed mx-auto mb-12 max-w-[1000px] text-center">
          <p class="eyebrow mb-4">// who it's for</p>
          <h2 class="font-display text-3xl font-bold text-primary md:text-4xl mb-4">
            Built for <span class="text-gradient">every stakeholder</span>
          </h2>
          <p class="mx-auto max-w-2xl text-lg text-secondary">
            The same intelligence layer, tuned to how you work — whether you develop projects, consult on studies, or
            operate the grid.
          </p>
        </div>
```

Leave the `.tabs` component (tab nav + the three `tabs-content` panels) unchanged — it keeps working via `initTabs()`.

- [ ] **Step 2: Build and visually verify**

Run: `npm run build-css`
Then open the site and scroll to the Solutions/"Who it's for" section.
Expected: build succeeds; the header now reads "// who it's for" + "Built for every stakeholder" (gradient on "every stakeholder") in Space Grotesk; the Developers / Consulting / ISO-Utilities tabs still switch correctly on click.

- [ ] **Step 3: Commit**

```bash
git add index.html assets/css/main.css
git commit -m "feat: reframe Solutions section as 'Who it's for'"
```

---

## Task 9: Restyle kept sections to the new type system

**Files:**
- Modify: `index.html` (Community, Clients, Support, Team, Contact headings)

- [ ] **Step 1: Add `font-display` to kept section headings and eyebrows where present**

In `index.html`, apply these targeted class additions (add `font-display` to each `<h2>`; convert the colored sub-labels to the `.eyebrow` style). Make these specific edits:

1. **Community** — the `<h6 class="mb-6 block text-3xl font-semibold text-accent">Our Ecosystem</h6>`: change its classes to `class="eyebrow mb-6 block text-base"`.
2. **Clients** — `<h2 class="mb-4 text-3xl font-bold text-primary">Trusted by Engineers at</h2>`: add `font-display` → `class="mb-4 font-display text-3xl font-bold text-primary"`.
3. **Support** — `<h2 class="mb-4 text-3xl font-bold text-primary">Supported by</h2>`: add `font-display`.
4. **Team** — the `<h6 ...>Our Team</h6>`: change to `class="eyebrow mb-2 block text-base"`. The `<h2 ...>Meet Our Core Team Members</h2>`: add `font-display`.
5. **Contact** — `<h2 class="mb-4 text-3xl font-bold leading-tight text-white sm:text-4xl">Ready to Accelerate Your Projects?</h2>`: add `font-display` and change the text to `Build on the intelligence layer for the grid.`

- [ ] **Step 2: Update the Community section intro copy to reinforce pillar 3**

In `index.html`, in the Community section, change the paragraph `Building on the Success of the First Open-Source Community for Power Industry` to:

```html
          <p class="text-secondary">
            Pillar three in practice: a human-in-the-loop community, building in the open. PowerAgent is the first
            open-source community for the power industry — and the foundation Semagram is built on.
          </p>
```

- [ ] **Step 3: Build and visually verify**

Run: `npm run build-css`
Then open the site and scroll through Community → Clients → Support → Team → Contact.
Expected: build succeeds; all section headings render in Space Grotesk (visibly more geometric than Inter); "Our Ecosystem" and "Our Team" now appear as small mono uppercase eyebrows in accent blue; the closing CTA heading reads "Build on the intelligence layer for the grid."; logo walls, team photos, newsletter form all still present and intact.

- [ ] **Step 4: Commit**

```bash
git add index.html assets/css/main.css
git commit -m "feat: restyle kept sections to Space Grotesk + eyebrow labels"
```

---

## Task 10: Final build, dist regeneration, and full smoke test

**Files:**
- No source edits expected; this task validates the whole site and the deploy artifact.

- [ ] **Step 1: Clean production build**

Run: `npm run build`
Expected: Tailwind compiles, then `dist/` is regenerated from scratch with `index.html` + `assets` + `semagram_logo`. No errors.

- [ ] **Step 2: Serve the dist build and full visual smoke test**

Serve the build (e.g. `npx serve dist`) and open the printed URL. Walk the whole page and confirm:
- Intro: grid fades in, status readout cycles, nodes pop, edges draw, copy reveals, then cursor-reactive graph + spotlight + traveling pulse work.
- Nav: Platform / How it works / Who it's for / Community / Team / Contact all smooth-scroll to the right sections; scrollspy highlights the active link.
- Sections present in order: hero → platform (stack) → pillars (3 cards w/ glow) → who it's for (tabs switch) → clients (logos) → community (PowerAgent + GitHub button) → support (backers) → team → contact (CTA + newsletter) → footer.
- Mobile: shrink the window; the hamburger menu opens/closes the nav; layouts stack; the hero graph still renders without overlapping the copy unreadably.
- Reduced motion: with emulation on, intro is skipped and content shows immediately.
Expected: all the above pass.

- [ ] **Step 3: Confirm no leftover references to removed elements**

Run: `grep -n "page-loading\|grid-loader\|Accelerating Access" index.html`
Expected: no matches (the old loader markup and old tagline are fully removed). If any match, remove it and re-run `npm run build`.

- [ ] **Step 4: Commit any final artifacts**

```bash
git add -A
git commit -m "chore: production build for rebrand" || echo "nothing to commit"
```

- [ ] **Step 5: Deploy (only when the user approves)**

Deployment is outward-facing — confirm with the user before running. When approved:
Run: `npm run deploy`
Expected: `npm run build` then `wrangler deploy` uploads `dist/` to Cloudflare; note the deployed URL and verify the live site.

---

## Self-Review Notes (author checklist — completed)

- **Spec coverage:** narrative/positioning → Tasks 3,4,6,7; vision-first + capabilities-lead IA → Tasks 4,6,7,8; light technical aesthetic + cyan token → Tasks 1,2; Space Grotesk/Inter/JetBrains Mono → Tasks 1,2,4,6–9; grid-bg + gradient + eyebrow utilities → Task 2; cursor-reactive hero → Task 5; graph self-construction intro + reduced-motion + replaces grid-loader → Tasks 4,5; pillar cards + glow → Tasks 2,7; reuse Stack diagram → Task 6; kept sections (clients/community/support/team/contact/newsletter) → Tasks 8,9; nav + scrollspy ids → Task 3 (+ verified Task 10); single static index.html + Tailwind build caveat + content-scan caveat → header + every build step; SEO/OG → Task 3. No spec section left without a task.
- **Placeholder scan:** no TBD/TODO/"add error handling"/"similar to Task N" — all steps carry exact code or exact edits.
- **Type/selector consistency:** data hooks used consistently across Tasks 4 & 5 (`[data-hero-net]`, `[data-hero-grid]`, `[data-hero-spot]`, `[data-hero-reveal]`, `[data-hero-status]`, `[data-hero-status-text]`, `[data-hero-status-pct]`); CSS classes (`.grid-bg`, `.eyebrow`, `.text-gradient`, `.reveal-mask`, `.reveal-fade`, `.is-revealed`, `.hero-graph`, `.hero-spot`, `.hero-status`, `.is-on`, `.pillar-card`, `.stack-row`, `.interactive-glow`) defined in Task 2 and used verbatim in Tasks 4,6,7. Anchor ids (`#platform`, `#pillars`, `#solutions`) created in Tasks 6,7,8 match nav hrefs from Task 3.
